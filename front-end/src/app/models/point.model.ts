export class Point {
    public lng: number;
    public lan: number;
    public elevation: number;
    public speedLimit: number;
    constructor(lng: number, lan: number) {
      this.lng = lng;
      this.lan = lan;
    }
}
